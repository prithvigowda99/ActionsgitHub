name: demo1

on: workflow_dispatch

jobs: 
    this-is-my-first:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: echo "${{ github.actor }} welcome to git hub actions"
            - run: ls -lart